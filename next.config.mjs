// @ts-check
import { initOpenNextCloudflareForDev } from "@opennextjs/cloudflare";

// Initialize the Cloudflare adapter for local `next dev`
initOpenNextCloudflareForDev();

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
